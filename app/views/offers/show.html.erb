<div class= "container">

  <div class= "card-show">
        <!-- Si l'offre de l'utilisateur appartient déjà à une liste
              crée une wishlist (coeur blanc)
             Autrement, l'offre est déjà présente dans la wishlist
             et il faut donc le supprimer (coeur rose) -->
          <div class="coeur-fav-profil">
             <% if current_user.already_in_list?(@offer) %>
              <%= link_to wishlist_path(current_user.get_wishlist(@offer)), method: :delete, remote: true do %>
                  <%= image_tag "coeur_fav.png", alt: "coeur rose" %>
              <% end %>

             <% else %>
               <%= link_to offer_wishlists_path(@offer), method: :post, remote: true do %>
                  <%= image_tag "coeur_blanc.png", alt: "coeur blanc" %>
               <% end %>
            <% end %>
          </div>

    <p><%= @offer.name %></p>
    <p><%= @offer.company.name %></p>
    <p><%= @offer.contract %></p>
    <p><%= @offer.income %></p>
    <p><%= @offer.description %></p>

  </div>
  <%= link_to "Revenir à la liste d'offres", offers_path %>
</div>
